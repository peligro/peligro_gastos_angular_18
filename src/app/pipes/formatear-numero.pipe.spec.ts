import { FormatearNumeroPipe } from './formatear-numero.pipe';

describe('FormatearNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
